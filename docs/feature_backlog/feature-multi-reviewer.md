# Feature: Multi-Reviewer Support

## Summary

Allow users to select multiple reviewers at once instead of being limited to a single reviewer per submission. Each selected reviewer runs independently and returns its own set of feedback, giving the user a richer, multi-perspective critique of their writing.
